---
apiVersion: v1
kind: Service
metadata:
  name: jellystat
  namespace: jellystat
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: jellystat
